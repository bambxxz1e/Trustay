enum NavigationType {
  /// 현재 화면 위에 새 화면을 쌓음
  /// - 이전 화면이 네비게이션 스택에 남아있음
  /// - 뒤로가기 가능
  push,

  /// 현재 화면을 제거하고 새 화면으로 교체
  /// - 이전 화면이 스택에서 사라짐
  /// - 뒤로가기 불가능
  replace,

  /// 네비게이션 스택을 전부 비우고 새 화면만 남김
  /// - 이전 화면 전부 제거
  /// - 뒤로가기 완전 차단
  clearStack,
}
